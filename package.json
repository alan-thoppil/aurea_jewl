{
  "name": "aurea-jewelpro-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend/aureaa-main\"",
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend/aureaa-main\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend/aureaa-main && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
